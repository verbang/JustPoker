## 1. CSS 布局修复

- [x] 1.1 修改 `RoomView.vue` 中横屏媒体查询，将 `.table-zone` 的 `align-items` 改为 `flex-start`

## 2. 验证

- [x] 2.1 在横屏模式下测试比分板和牌型参考面板展开时，左侧牌桌是否保持顶部对齐
- [x] 2.2 在竖屏模式下测试布局是否保持原有行为
- [x] 2.3 在不同屏幕尺寸下测试布局是否正常显示
